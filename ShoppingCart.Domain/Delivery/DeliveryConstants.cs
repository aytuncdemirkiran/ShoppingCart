using System;

namespace ShoppingCart.Domain.Delivery
{

    
    public static class DeliveryConstants
    {
        public const decimal FixedCostAmount = (decimal)2.99;
        public const decimal CostPerProduct = (decimal)1.99;
        public const decimal CostPerDelivery = (decimal)1.99;
    }
}